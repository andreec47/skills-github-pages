<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Monedas TikTok</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f7f7f7;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      padding: 40px;
      text-align: center;
      max-width: 400px;
      width: 90%;
    }

    .logo {
      font-size: 24px;
      font-weight: bold;
      color: #ff0050;
      margin-bottom: 20px;
    }

    .coins {
      font-size: 32px;
      color: #222;
      margin-bottom: 10px;
    }

    .label {
      color: #888;
      font-size: 14px;
    }

    .btn {
      margin-top: 20px;
      background-color: #ff0050;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
    }

    .btn:hover {
      background-color: #e60045;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">TikTok Monedas</div>
    <div class="coins">2,046,944</div>
    <div class="label">Monedas disponibles</div>
    <button class="btn">Recargar más</button>
  </div>
</body>
</html>